name: kv_module
version: 0.1.0
type: core
category: storage
description: "Local key-value storage module for Logos Core with swappable backends (memory/file/rocksdb/sqlite)"
nix_packages:
  build:
    - qt6.qtbase
  runtime: []
